import React from "react"

export default function CartPage({showCart}){
    return(
        <>
            {/* A whole page where it shows all the experiences clicked */}
            
             
        </>
    ) 
}